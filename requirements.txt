# Core dependencies for ATLAS. Versions match the environment used in the paper
# (Python 3.9.5, CUDA 12.4); nearby versions generally work.
# `requirements-full.txt` is the exact frozen environment.

torch==2.6.0
transformers==4.54.0
accelerate==1.9.0
tokenizers==0.21.2
safetensors==0.5.3
timm==1.0.19
einops==0.8.1

numpy==2.0.2
pandas==2.3.1
scipy==1.13.1
scikit-learn==1.6.1
pyarrow==20.0.0

PyYAML==6.0.2
tqdm==4.67.1
wandb==0.21.1

# Autoencoder evaluation metrics
nltk==3.9.1
Levenshtein==0.27.1

# Plots in evaluation outputs
matplotlib==3.9.4
seaborn==0.13.2

# Optional: exact point-in-polygon georegion assignment for Foursquare NYC
# (step 05 falls back to an approximate lat/lon rule without these)
geopandas==1.0.1
shapely==2.0.7
pyproj==3.6.1
